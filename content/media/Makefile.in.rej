***************
*** 93,112 ****
  ifdef MOZ_DASH
  PARALLEL_DIRS += dash
  endif
  
  ifdef MOZ_MEDIA_PLUGINS
  PARALLEL_DIRS += plugins
  endif
  
- ifdef MOZ_WMF
- PARALLEL_DIRS += wmf
- endif
- 
  PARALLEL_DIRS += webrtc
  
  ifeq (gonk,$(MOZ_WIDGET_TOOLKIT))
  PARALLEL_DIRS += omx
  endif
  
  TEST_DIRS += test
  
--- 93,108 ----
  ifdef MOZ_DASH
  PARALLEL_DIRS += dash
  endif
  
  ifdef MOZ_MEDIA_PLUGINS
  PARALLEL_DIRS += plugins
  endif
  
  PARALLEL_DIRS += webrtc
  
  ifeq (gonk,$(MOZ_WIDGET_TOOLKIT))
  PARALLEL_DIRS += omx
  endif
  
  TEST_DIRS += test
  
